import type React from "react"
// Este arquivo não é mais necessário, pois a lógica foi movida para o layout principal

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
